@page
@model deliverycart1.Pages_SHopping.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Vendor[0].storeName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Vendor[0].storeAddress)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Vendor[0].storeCity)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Vendor[0].storeState)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Vendor[0].storeZip)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Vendor) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.storeName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.storeAddress)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.storeCity)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.storeState)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.storeZip)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.VendorID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.VendorID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.VendorID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
